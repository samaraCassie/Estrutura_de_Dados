#include <iostream>
#include "Math.h"
using namespace std;

float PI = Math.PI;


void CalcularArea(float raio){
    *area = PI * (raio**);
    cout << "\nArea do circulo: " << *area;
}


void main(){
    double area, raio;

    cout << "Raio do circulo: \n";
    cin >> raio;

    CalcularArea(raio);
}
